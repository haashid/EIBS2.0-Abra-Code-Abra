
record TextStats {
    word_count: uint,
    char_count: uint,
    line_count: uint,
    avg_word_length: float,
    input_hash: string
}

interface TextProcessor {
    query func get_stats() -> uint;
    mutate func execute(input: string) -> string;
    mutate func process_text(text: string) -> TextStats
}

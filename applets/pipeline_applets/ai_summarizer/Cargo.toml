[package]
name = "ai_summarizer"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Add weil-sdk dependency when available
# weil-sdk = "0.1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
